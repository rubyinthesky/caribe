<%= form_with(model: hasht, local: true) do |form| %>
  <% if hasht.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hasht.errors.count, "error") %> prohibited this hasht from being saved:</h2>

      <ul>
      <% hasht.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
